Function is running in Google Cloud

Need these env variables set

token = notion token taken from session cookie
page = page url we want to add the entry to
api_key = random api key to prevent access

Testing locally
Make run.sh file and add the following content setting the correct env vars
----
export token=""
export page=""
export api_key=''

gcpfemu --module updateTable.py --function updateTable 
----
Change permission and you can run it directly to test locally.
